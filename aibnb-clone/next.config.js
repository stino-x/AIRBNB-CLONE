/** @type {import('next').NextConfig} */
// next.config.js

const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'avatar.iran.liara.run', 'lh3.googleusercontent.com'],
  },
};

module.exports = nextConfig;
